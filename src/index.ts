import { parseParams, parseQuery, parseForm } from './parsers';
import {
  BoolAsString,
  CheckboxAsString,
  IntAsString,
  NumAsString,
} from './schemas';

export {
  parseParams,
  parseQuery,
  parseForm,
  BoolAsString,
  CheckboxAsString,
  IntAsString,
  NumAsString,
};

export const zx = {
  parseParams,
  parseQuery,
  parseForm,
  BoolAsString,
  CheckboxAsString,
  IntAsString,
  NumAsString,
};
