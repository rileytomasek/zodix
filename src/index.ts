import {
  parseParams,
  parseParamsSafe,
  parseQuery,
  parseQuerySafe,
  parseForm,
  parseFormSafe,
} from './parsers';
import {
  BoolAsString,
  CheckboxAsString,
  IntAsString,
  NumAsString,
} from './schemas';

export {
  parseParams,
  parseParamsSafe,
  parseQuery,
  parseQuerySafe,
  parseForm,
  parseFormSafe,
  BoolAsString,
  CheckboxAsString,
  IntAsString,
  NumAsString,
};

export const zx = {
  parseParams,
  parseParamsSafe,
  parseQuery,
  parseQuerySafe,
  parseForm,
  parseFormSafe,
  BoolAsString,
  CheckboxAsString,
  IntAsString,
  NumAsString,
};
